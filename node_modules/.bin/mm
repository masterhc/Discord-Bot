#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../music-manager/music-manager" "$@"
  ret=$?
else 
  node  "$basedir/../music-manager/music-manager" "$@"
  ret=$?
fi
exit $ret
